name: Generate

on:
  schedule:
    - cron: '0 0 * * 6'
  workflow_dispatch:
    inputs:
      ooui-tag:
        description: 'OOUI Tag'     
        required: false
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      VERSION: Unknown

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 16
      - uses: php-actions/composer@v6
        with:
          php_version: 7.4
          command: ''
      - run: make setup TAG=${{ github.event.inputs.ooui-tag }}
      - run: make create
      - run: make convert
      - run: make build
      - run: make copy
      - name: Get version
        run: |
          echo "VERSION=$(grep -oP 'OOUI \Kv.*' dist/resources/ooui/oojs-ui-core-dye.css)" >> $GITHUB_ENV
      - name: Create pull request
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: Generate ${{ env.VERSION }}
          title: Generate ${{ env.VERSION }}
          body: ''
          delete-branch: true
          branch: generate
      
